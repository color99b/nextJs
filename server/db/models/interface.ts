export interface IItem extends Document {
  name: string;
  description: string;
  price: number;
}

export interface ISchema extends Document {
  name: string;
  description: string;
  price: number;
}
